name: Refine RxNXT homepage

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  refine:
    if: contains(github.event.head_commit.message, 'Trigger homepage refinement')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Refine hero messaging and visitor flow
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text()

          old_h1 = '''<h1>Don't let the prescription be the <span>end of the consultation.</span></h1>'''
          new_h1 = '''<h1>Don't let the prescription be the <span>end of the consultation.</span><strong class="heroSub">Make it the beginning of treatment.</strong></h1>'''
          if old_h1 in s:
              s = s.replace(old_h1, new_h1, 1)

          old_css = '''.hero h1 span{color:var(--green)}.heroLead{font-size:18px;color:var(--muted);max-width:650px;margin:24px 0 28px}.actions{display:flex;gap:11px;flex-wrap:wrap}'''
          new_css = '''.hero h1 span{color:var(--green)}.heroSub{display:block;font-size:.58em;line-height:1.08;color:var(--ink);margin-top:18px;letter-spacing:-.04em}.heroSub::first-letter{color:var(--green)}.heroLead{font-size:18px;color:var(--muted);max-width:650px;margin:24px 0 18px}.heroNext{display:flex;align-items:center;gap:10px;margin:0 0 25px;color:#536862}.heroNext span{width:28px;height:28px;border-radius:50%;display:grid;place-items:center;background:#dff3e9;color:var(--green);font-weight:800}.heroNext strong{font-size:12px;color:var(--ink)}.heroNext small{font-size:10px;color:var(--muted)}.actions{display:flex;gap:11px;flex-wrap:wrap}'''
          if old_css in s:
              s = s.replace(old_css, new_css, 1)

          old_lead = '''<p class="heroLead">Make it the beginning of treatment. RxNXT helps doctors carry the treatment journey beyond the consultation—with digital prescriptions, medication reminders, follow-up and patient engagement.</p><div class="actions">'''
          new_lead = '''<p class="heroLead">RxNXT helps doctors carry the treatment journey beyond the consultation—with digital prescriptions, medication reminders, follow-up and patient engagement.</p><div class="heroNext"><span>↓</span><strong>See what happens after the consultation</strong><small>Scroll to follow the treatment journey</small></div><div class="actions">'''
          if old_lead in s:
              s = s.replace(old_lead, new_lead, 1)

          old_section = '''<section class="section question" id="why"><div class="wrap"><div class="intro"><div class="kicker">The moment after prescribing</div><h2>The consultation ends. The treatment doesn't.</h2><p>A prescription creates the plan. What happens after the patient leaves the clinic is where treatment has to continue.</p></div>'''
          new_section = '''<section class="section question" id="why"><div class="wrap"><div class="intro"><div class="kicker">What happens next?</div><h2>The consultation ends. The treatment doesn't.</h2><p>A prescription creates the plan. But once the patient leaves the clinic, the treatment journey has only just begun.</p></div>'''
          if old_section in s:
              s = s.replace(old_section, new_section, 1)

          p.write_text(s)
          PY

      - name: Commit homepage update and remove one-time workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html .github/workflows/refine-homepage.yml
          git commit -m "Update RxNXT hero messaging" || exit 0
          git push
          git rm .github/workflows/refine-homepage.yml
          git commit -m "Remove one-time homepage refinement workflow"
          git push
